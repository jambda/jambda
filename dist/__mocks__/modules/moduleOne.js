export const onLoad = jest.fn((app, config) => {
    return app
})

export const onBeforeRoute = jest.fn((app, config) => {
    return app
})

export const registerRoutes = jest.fn((app, config) => {
    return app
})

export const onAfterRoute = jest.fn((app, config) => {
    return app
})

export const onError = jest.fn((app, config) => {
    return app
})
