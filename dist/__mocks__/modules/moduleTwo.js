export const onLoad = jest.fn((app, config) => {
    return app
})

export const onBeforeRoute = jest.fn((app, config) => {
    return app
})

export const registerRoutes = jest.fn((app, config) => {
    return app
})
